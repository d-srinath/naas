Thanks for the update. From OpenShift/platform side, everything looks good — proxy configuration and connectivity are working as expected.

Since the request is being blocked by corporate policy, this is not an OpenShift issue. The Google API endpoint needs to be whitelisted by the security/network team for outbound access.

No additional changes are required from the OpenShift side at this point
